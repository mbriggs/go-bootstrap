#!/usr/bin/env bash
#
# Creates a new migration: bin/migration add_things_table

set -euo pipefail
cd "$(dirname "$0")/.."

if [ $# -ne 1 ]; then
  echo "usage: bin/migration <name>" >&2
  exit 1
fi

go run ./cmd/migrate create "$1"
