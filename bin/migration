#!/usr/bin/env bash
#
# Creates a new migration: bin/migration add_things_table

set -euo pipefail
cd "$(dirname "$0")/.."

if [ $# -ne 1 ]; then
  echo "usage: bin/migration <name>" >&2
  exit 1
fi

tern new -m "${TERN_MIGRATIONS:-migrations}" "$1"
